os: linux
language: java
jdk:
  - openjdk11
services:
  - docker
cache:
  directories:
    - $HOME/.m2
before_install:
  - docker pull openjdk:11
  - chmod +x ./travis-build.sh
  - chmod +x ./travis-deploy.sh
script:
  - ./travis-build.sh
after_success:
  - ./travis-deploy.sh